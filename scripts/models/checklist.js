/**
* Class: An object that represents a checklist and its requirements 
* @param version 
*/
var Checklist = function(version) {
  this.checklist_version = version;
  this.checklist_rules = ;
  //Hosts a list of rules (Strings) -> classes it fulfills (String List)
  //TODO save user function
  function calculate_rules() {
    return {"CS 1110" : ["CS1110"],






            };
  }

};